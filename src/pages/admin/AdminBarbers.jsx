import { useEffect, useState } from "react";
import { barbersAPI } from "../../services/api";
import { adminBarbersAPI } from "../../services/adminAPI";

export default function AdminBarbers(){const[items,setItems]=useState([]);const[form,setForm]=useState({name:"",specialty:"",photo_url:""});async function load(){const{data}=await barbersAPI.getAll();setItems(data.barbers||[]);}useEffect(()=>{load();},[]);async function submit(e){e.preventDefault();await adminBarbersAPI.create(form);setForm({name:"",specialty:"",photo_url:""});load();}async function remove(id){await adminBarbersAPI.remove(id);load();}return <section className="admin-page"><h1>Barbeiros</h1><form className="admin-card admin-form" onSubmit={submit}><input placeholder="Nome" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/><input placeholder="Especialidade" value={form.specialty} onChange={e=>setForm({...form,specialty:e.target.value})}/><input placeholder="URL da foto" value={form.photo_url} onChange={e=>setForm({...form,photo_url:e.target.value})}/><button>Adicionar</button></form><div className="admin-card admin-table-wrap"><table className="admin-table"><thead><tr><th>Nome</th><th>Especialidade</th><th></th></tr></thead><tbody>{items.map(b=><tr key={b.id}><td>{b.name}</td><td>{b.specialty||"-"}</td><td><button className="danger-btn" onClick={()=>remove(b.id)}>Excluir</button></td></tr>)}</tbody></table></div></section>}
